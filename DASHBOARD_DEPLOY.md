# 📊 Dashboard 可视化部署指南

## 🎯 功能说明

Dashboard 是一个可视化页面，用于展示：
- 📈 推文数据统计（总数、用户数、图片/视频数量）
- 📅 每日推文趋势图
- 👥 用户活跃度排行
- 🎨 美观的现代化 UI 界面

## 🚀 部署步骤

### 方法一：GitHub Pages 自动部署（推荐）

#### 1. 启用 GitHub Pages

1. 进入仓库 `Settings` → `Pages`
2. 在 `Source` 中选择 `GitHub Actions`
3. 保存设置

#### 2. 配置 Secrets

确保已配置 `GH_TOKEN`（参考 GITHUB_ACTIONS_DEPLOY.md）

#### 3. 推送代码

```bash
git add .
git commit -m "feat: add dashboard deployment"
git push origin main
```

#### 4. 触发部署

- **自动触发：** 每6小时自动更新
- **手动触发：** 
  - 进入 `Actions` → `Deploy Dashboard` → `Run workflow`
- **推送触发：** 当 `tweets/` 目录有更新时自动部署

#### 5. 访问 Dashboard

部署完成后，访问：
```
https://你的用户名.github.io/你的仓库名/
```

例如：`https://username.github.io/x-kit/`

---

### 方法二：本地生成和预览

#### 1. 生成 Dashboard

```bash
bun run scripts/generate-dashboard.ts
```

#### 2. 预览

Dashboard 会生成在 `dashboard/index.html`

- **使用 Python：**
  ```bash
  cd dashboard
  python3 -m http.server 8000
  ```
  访问：http://localhost:8000

- **使用 Bun：**
  ```bash
  bunx serve dashboard
  ```

- **使用 VS Code：**
  - 安装 `Live Server` 扩展
  - 右键 `dashboard/index.html` → `Open with Live Server`

---

## 🔧 自定义配置

### 修改更新频率

编辑 `.github/workflows/deploy-dashboard.yml`：

```yaml
schedule:
  - cron: '0 */6 * * *'  # 每6小时
  # 其他选项：
  # '0 0 * * *'     # 每天0点
  # '0 */12 * * *'  # 每12小时
  # '0 9 * * 1'     # 每周一9点
```

### 修改显示数量

编辑 `scripts/generate-dashboard.ts`：

```typescript
.slice(0, 50)  // 修改为显示前N名用户
.slice(-30)    // 修改为显示最近N天的数据
```

### 自定义样式

编辑 `scripts/generate-dashboard.ts` 中的 CSS 部分，修改：
- 颜色主题
- 布局样式
- 字体大小
- 动画效果

---

## 📱 功能特性

### 1. 统计卡片
- 总推文数
- 用户数量
- 包含图片的推文数
- 包含视频的推文数

### 2. 每日趋势图
- 最近30天的推文数量
- 可视化柱状图
- 鼠标悬停显示详情

### 3. 用户排行
- 按推文数量排序
- 显示用户头像、名称、用户名
- 显示粉丝数、关注数
- 显示该用户的推文数量

---

## 🐛 常见问题

### Q1: Dashboard 页面空白

**解决方案：**
- 检查 `tweets/` 目录是否有数据文件
- 检查 GitHub Actions 日志是否有错误
- 确保 JSON 文件格式正确

### Q2: GitHub Pages 404 错误

**解决方案：**
1. 检查仓库设置 → Pages → Source 是否为 `GitHub Actions`
2. 检查 Actions 是否成功运行
3. 等待几分钟后刷新（部署需要时间）

### Q3: 样式显示异常

**解决方案：**
- 检查浏览器控制台是否有错误
- 清除浏览器缓存
- 确保 HTML 文件完整生成

### Q4: 数据不更新

**解决方案：**
- 检查 `get-home-latest-timeline.yml` 是否正常运行
- 手动触发 Dashboard 部署
- 检查推文文件是否在 `tweets/` 目录

---

## 🎨 自定义主题

### 修改颜色

在 `generate-dashboard.ts` 中找到颜色定义：

```typescript
// 主色调
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
color: #667eea;

// 修改为你喜欢的颜色
background: linear-gradient(135deg, #你的颜色1 0%, #你的颜色2 100%);
```

### 修改布局

调整 CSS Grid 布局：

```css
/* 统计卡片布局 */
grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));

/* 用户列表布局 */
grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
```

---

## 📊 数据来源

Dashboard 从以下位置读取数据：
- `tweets/*.json` - 推文数据文件
- `accounts/*.json` - 用户信息（可选）

---

## 🔄 更新流程

1. **数据采集** → `get-home-latest-timeline.yml` 每30分钟运行
2. **数据存储** → 保存到 `tweets/YYYY-MM-DD.json`
3. **Dashboard 生成** → `deploy-dashboard.yml` 每6小时运行
4. **自动部署** → 推送到 GitHub Pages

---

## 📚 相关文档

- [GitHub Actions 部署指南](./GITHUB_ACTIONS_DEPLOY.md)
- [快速启动指南](./QUICK_START.md)
- [GitHub Pages 文档](https://docs.github.com/en/pages)

---

## 💡 进阶功能（可选）

### 添加搜索功能

可以在 HTML 中添加搜索框，使用 JavaScript 过滤用户列表。

### 添加数据导出

可以添加 CSV/JSON 导出功能，方便数据分析。

### 添加时间范围筛选

可以添加日期选择器，查看特定时间段的数据。

### 集成图表库

可以使用 Chart.js 或 ECharts 替换简单的柱状图，提供更丰富的可视化效果。

