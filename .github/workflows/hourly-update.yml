name: Hourly Update

on:
  schedule:
    - cron: '0 * * * *'  # 每小时的第0分钟执行（即每小时执行一次）
  workflow_dispatch:      # 允许手动触发

jobs:
  update-tweets:
    name: 获取最新推文
    runs-on: ubuntu-latest
    timeout-minutes: 10  # 设置超时时间，避免长时间运行
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_TOKEN || github.token }}
          fetch-depth: 0  # 获取完整历史记录
          
      - name: Setup Git
        run: |
          git config --global user.name 'GitHub Actions Bot'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          
      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest
          
      - name: Install dependencies
        run: bun install
        
      - name: Verify AUTH_TOKEN is configured
        run: |
          if [ -z "${{ secrets.AUTH_TOKEN }}" ]; then
            echo "❌ 错误: AUTH_TOKEN Secret 未配置！"
            echo ""
            echo "请按照以下步骤配置："
            echo "1. 进入仓库 Settings → Secrets and variables → Actions"
            echo "2. 点击 New repository secret"
            echo "3. Name: AUTH_TOKEN"
            echo "4. Secret: 你的 auth_token 值"
            exit 1
          else
            echo "✅ AUTH_TOKEN 已配置"
            echo "Token 长度: ${#AUTH_TOKEN}"
          fi
        env:
          AUTH_TOKEN: ${{ secrets.AUTH_TOKEN }}
        
      - name: Fetch latest tweets
        env:
          AUTH_TOKEN: ${{ secrets.AUTH_TOKEN }}
        run: |
          echo "开始获取最新推文..."
          echo "Token 前10位: ${AUTH_TOKEN:0:10}..."
          bun run scripts/fetch-tweets.ts
          echo "推文获取完成"
        
      - name: Check for changes
        id: verify-changed-files
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "检测到文件变更"
          else
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "没有文件变更"
          fi
        
      - name: Commit and push changes
        if: steps.verify-changed-files.outputs.changed == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN || github.token }}
        run: |
          git add tweets/ accounts/ || true
          git commit -m "chore: update tweets [skip ci] - $(date '+%Y-%m-%d %H:%M:%S')" || exit 0
          git remote set-url origin https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git
          git push || echo "推送失败，可能是没有变更"
          
      - name: Summary
        if: always()
        run: |
          echo "## 📊 执行结果" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ 执行时间: $(date '+%Y-%m-%d %H:%M:%S')" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.verify-changed-files.outputs.changed }}" == "true" ]; then
            echo "- ✅ 数据已更新并提交" >> $GITHUB_STEP_SUMMARY
          else
            echo "- ℹ️ 没有新数据" >> $GITHUB_STEP_SUMMARY
          fi

