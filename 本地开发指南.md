# 🛠️ 本地开发 Dashboard 完整指南

## 🎯 目标

在本地生成和调试 Dashboard HTML 文件，完善后再发布到 GitHub Pages。

---

## ✅ 快速开始（3步）

### 步骤 1：生成 Dashboard HTML

```bash
# 使用 Bun（如果已安装）
bun run scripts/generate-dashboard.ts

# 或使用增强版（功能更丰富）
bun run scripts/generate-dashboard-enhanced.ts
```

**输出位置：** `dashboard/index.html`

### 步骤 2：本地预览

```bash
# 进入 dashboard 目录
cd dashboard

# 使用 Python 启动本地服务器
python3 -m http.server 8000

# 或使用 Python 2
python -m SimpleHTTPServer 8000
```

### 步骤 3：在浏览器中打开

访问：`http://localhost:8000`

---

## 📁 文件说明

### 基础版 Dashboard

**文件：** `scripts/generate-dashboard.ts`

**功能：**
- ✅ 基本统计卡片
- ✅ 简单的柱状图
- ✅ 用户排行列表

### 增强版 Dashboard（推荐）

**文件：** `scripts/generate-dashboard-enhanced.ts`

**新增功能：**
- ✅ Chart.js 图表（更美观的折线图）
- ✅ 搜索功能（搜索用户）
- ✅ 最新推文展示
- ✅ 用户卡片点击跳转
- ✅ 更好的交互体验

---

## 🔄 开发工作流

### 1. 修改代码

编辑生成脚本：
- `scripts/generate-dashboard.ts` - 基础版
- `scripts/generate-dashboard-enhanced.ts` - 增强版

### 2. 重新生成

```bash
bun run scripts/generate-dashboard-enhanced.ts
```

### 3. 刷新浏览器

在浏览器中按 `F5` 或 `Cmd+R` 刷新页面。

### 4. 调试

- 按 `F12` 打开开发者工具
- 查看 Console 是否有错误
- 检查 Network 资源加载
- 使用 Elements 检查 HTML 结构

### 5. 重复步骤 1-4

直到满意为止。

### 6. 提交代码

```bash
git add scripts/generate-dashboard-enhanced.ts
git commit -m "feat: add enhanced dashboard with search and charts"
git push origin main
```

---

## 🎨 自定义功能

### 添加搜索功能

在 HTML 中添加搜索框：

```html
<input type="text" id="searchInput" placeholder="搜索...">
<script>
  document.getElementById('searchInput').addEventListener('input', (e) => {
    const keyword = e.target.value.toLowerCase();
    // 过滤逻辑
  });
</script>
```

### 添加筛选功能

```javascript
function filterByDate(startDate, endDate) {
  // 筛选指定日期范围的推文
}

function filterByUser(username) {
  // 筛选特定用户的推文
}
```

### 添加排序功能

```javascript
function sortUsers(sortBy) {
  // 按推文数、粉丝数等排序
  if (sortBy === 'tweets') {
    // 按推文数排序
  } else if (sortBy === 'followers') {
    // 按粉丝数排序
  }
}
```

### 使用图表库

增强版已集成 Chart.js，你可以：

```javascript
// 创建更多图表类型
new Chart(ctx, {
  type: 'bar',  // 柱状图
  type: 'pie',  // 饼图
  type: 'doughnut',  // 环形图
  // ...
});
```

---

## 🔍 调试技巧

### 1. 查看控制台

按 `F12` → `Console` 标签：
- 查看 JavaScript 错误
- 打印调试信息：`console.log('数据:', data)`

### 2. 检查网络请求

按 `F12` → `Network` 标签：
- 查看资源加载情况
- 检查 API 请求（如果有）

### 3. 检查元素

按 `F12` → `Elements` 标签：
- 查看 HTML 结构
- 修改样式实时预览
- 检查 CSS 选择器

### 4. 测试不同数据

在脚本中限制数据量进行测试：

```typescript
// 只使用前 100 条推文测试
const testTweets = allTweets.slice(0, 100);
```

---

## 📊 增强功能建议

### 1. 添加数据导出

```javascript
function exportToCSV() {
  // 导出为 CSV
  const csv = convertToCSV(data);
  downloadFile(csv, 'tweets.csv');
}

function exportToJSON() {
  // 导出为 JSON
  const json = JSON.stringify(data, null, 2);
  downloadFile(json, 'tweets.json');
}
```

### 2. 添加时间范围选择

```html
<input type="date" id="startDate">
<input type="date" id="endDate">
<button onclick="filterByDateRange()">筛选</button>
```

### 3. 添加分页功能

```javascript
function paginateUsers(page, pageSize) {
  // 分页显示用户列表
}
```

### 4. 添加数据刷新

```javascript
function refreshData() {
  // 重新加载数据
  location.reload();
}
```

---

## 🐛 常见问题

### Q1: 图片无法显示

**原因：** 直接打开 HTML 文件，浏览器阻止加载外部图片。

**解决：** 使用本地服务器：
```bash
cd dashboard
python3 -m http.server 8000
```

### Q2: Chart.js 无法加载

**原因：** 网络问题或 CDN 被阻止。

**解决：** 
1. 检查网络连接
2. 使用本地 Chart.js 文件
3. 使用其他 CDN

### Q3: 样式不生效

**检查：**
1. CSS 语法是否正确
2. 浏览器缓存（按 `Ctrl+Shift+R` 强制刷新）
3. 选择器是否正确

### Q4: 数据不显示

**检查：**
1. `tweets/` 目录是否有 JSON 文件
2. JSON 文件格式是否正确
3. 控制台是否有错误信息

---

## 📚 参考资源

- [Chart.js 文档](https://www.chartjs.org/docs/)
- [MDN Web Docs](https://developer.mozilla.org/)
- [CSS Grid 指南](https://css-tricks.com/snippets/css/complete-guide-grid/)

---

## ✅ 开发检查清单

- [ ] 本地可以生成 HTML 文件
- [ ] 本地预览正常显示
- [ ] 样式符合预期
- [ ] 功能正常工作
- [ ] 搜索功能正常
- [ ] 图表正常显示
- [ ] 响应式设计（移动端适配）
- [ ] 浏览器兼容性测试
- [ ] 代码已提交并推送

---

## 🚀 下一步

1. **完善 Dashboard**
   - 添加更多功能
   - 优化样式
   - 提升用户体验

2. **测试**
   - 在不同浏览器测试
   - 测试移动端显示
   - 测试不同数据量

3. **部署**
   - 提交代码
   - 触发 GitHub Actions
   - 访问 GitHub Pages

---

**开始本地开发吧！** 🎨

