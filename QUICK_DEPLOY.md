# ⚡ 快速部署指南（5分钟上手）

## 🎯 核心步骤（3步完成）

### 步骤 1️⃣：配置 Secrets（2分钟）

1. **进入 GitHub 仓库设置**
   ```
   仓库 → Settings → Secrets and variables → Actions → New repository secret
   ```

2. **添加 3 个 Secrets：**

   | 名称 | 值 |
   |------|-----|
   | `AUTH_TOKEN` | `c6458d4841dc6d8289651c3b6e6b9c26d904b062` |
   | `GET_ID_X_TOKEN` | `c6458d4841dc6d8289651c3b6e6b9c26d904b062` |
   | `GH_TOKEN` | [获取方法见下方] |

3. **获取 GH_TOKEN：**
   - 访问：https://github.com/settings/tokens
   - 点击 `Generate new token (classic)`
   - 勾选 `repo` 和 `workflow` 权限
   - 复制生成的 Token（格式：`ghp_xxxxx`）

### 步骤 2️⃣：启用 Actions（1分钟）

1. **设置 Actions 权限**
   ```
   Settings → Actions → General → Workflow permissions
   选择：Read and write permissions
   勾选：Allow GitHub Actions to create and approve pull requests
   ```

2. **启用 Actions**
   - 点击仓库顶部的 `Actions` 标签
   - 如果提示启用，点击 `I understand my workflows, go ahead and enable them`

### 步骤 3️⃣：测试运行（2分钟）

1. **手动触发**
   ```
   Actions → Hourly Update → Run workflow → Run workflow
   ```

2. **查看结果**
   - 等待执行完成（约1-2分钟）
   - 检查 `tweets/` 目录是否有新文件
   - 绿色 ✅ 表示成功

---

## ✅ 完成！

配置完成后：
- ✅ 每小时自动执行一次
- ✅ 自动获取最新推文
- ✅ 自动提交到仓库

---

## 📋 配置文件位置

```
.github/workflows/hourly-update.yml
```

**执行时间：** 每小时的第0分钟（例如：00:00, 01:00, 02:00...）

---

## 🐛 遇到问题？

### 问题：Workflow 没有运行
- ✅ 检查 Secrets 是否配置
- ✅ 等待至少1小时（首次可能有延迟）
- ✅ 手动触发测试

### 问题：执行失败
- ✅ 查看 Actions 日志
- ✅ 检查 Token 是否过期
- ✅ 参考 [完整部署指南](./GITHUB_ACTIONS_SETUP.md)

---

## 📚 详细文档

- [完整部署指南](./GITHUB_ACTIONS_SETUP.md) - 详细步骤和故障排查
- [API 配置指南](./API_CONFIG.md) - Token 配置说明

---

**🎉 就这么简单！开始使用吧！**

